language: android
sudo: false
jdk: oraclejdk8

android:
  components:
    - platform-tools
    - tools
    - build-tools-26.0.1
    - android-26
    - extra-android-m2repository

script:
    - ./gradlew test

before_install:
    - chmod +x gradlew
